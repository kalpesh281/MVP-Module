# Secrets — never commit
.env
.env.local
*.pem

# Python
.venv/
__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/

# Node
node_modules/
dist/
.vite/

# OS
.DS_Store
.venv/

# Browser-verification artefacts. Written by the Playwright MCP server on
# every screenshot and snapshot — console logs, page trees, PNGs. Useful
# while checking a change, never part of the deliverable.
.playwright-mcp/
