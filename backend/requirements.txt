# --- web ---
fastapi>=0.115
uvicorn[standard]>=0.32
pydantic>=2.9
python-dotenv>=1.0

# --- scanning ---
checkdmarc>=5.17          # SPF + DMARC parsing and validation (pulls dnspython, publicsuffixlist)
dnspython>=2.7            # DKIM selector probing, async resolution
sslyze>=6.0               # TLS protocol enumeration, chain validation
httpx>=0.27               # async HTTP: headers check, crt.sh

# --- data ---
pymongo>=4.9              # AsyncMongoClient — NOT motor

# --- AI: install at least one ---
anthropic>=1.0
openai>=1.60

# --- dev ---
pytest>=8.0
pytest-asyncio>=0.24
respx>=0.21               # httpx mocking for fixtures
